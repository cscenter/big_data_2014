Шашкова Елизавета, CSC, 2014
Хранение и обработка больших объёмов данных, дз №3

Input:
Матрицы, записанные в файлах DFS(один файл содержит одну строку матрицы). 
Строка начинается с записи вида "l-num" или "r-num", где l или r определяет, слева или справа находится матрица при умножении. Число num обозначает номер строки в матрице.

Output:
Результирующая матрица возвращается построчно (возвращаемая информация - номер строки в матрице и последовательность чисел).  



